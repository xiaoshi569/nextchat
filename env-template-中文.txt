# OpenAI API 密钥（必需）
OPENAI_API_KEY=sk-xxxx

# DeepSeek API 密钥（可选）
DEEPSEEK_API_KEY=

# 访问密码，多个密码用逗号分隔（可选）
CODE=your-password

# 代理服务器地址（可选）
PROXY_URL=http://localhost:7890

# 启用 MCP 功能（可选）
# 默认：空（禁用）
# 设置为 true 启用 MCP 功能
ENABLE_MCP=

# Google Gemini Pro API 密钥（可选）
# 默认：空
# 如果要使用 Google Gemini Pro API，请设置此值
GOOGLE_API_KEY=

# Google Gemini Pro API 地址（可选）
# 默认：https://generativelanguage.googleapis.com/
# 如果要自定义 Google Gemini Pro API 地址，请设置此值（不包含路径）
GOOGLE_URL=

# 覆盖 OpenAI API 请求基础地址（可选）
# 默认：https://api.openai.com
# 示例：http://your-openai-proxy.com
BASE_URL=

# 指定 OpenAI 组织 ID（可选）
# 默认：空
OPENAI_ORG_ID=

# 禁用 GPT-4（可选）
# 默认：空
# 如果不想让用户使用 GPT-4，设置为 1
DISABLE_GPT4=

# 隐藏用户 API 密钥输入（可选）
# 默认：空
# 如果不想让用户输入自己的 API 密钥，设置为 1
HIDE_USER_API_KEY=

# 启用余额查询（可选）
# 默认：空
# 如果想让用户查询余额，设置为 1
ENABLE_BALANCE_QUERY=

# 禁用从 URL 解析设置（可选）
# 默认：空
# 如果要禁用从 URL 解析设置，设置为 1
DISABLE_FAST_LINK=

# 自定义模型列表（可选）
# 默认：空
# 使用 + 添加自定义模型，使用 - 隐藏模型，使用 name=displayName 自定义模型显示名称，用逗号分隔
CUSTOM_MODELS=

# 默认模型（可选）
# 默认：空
# 更改默认使用的模型
DEFAULT_MODEL=

# Anthropic Claude API 密钥（可选）
ANTHROPIC_API_KEY=

# Anthropic Claude API 版本（可选）
ANTHROPIC_API_VERSION=

# Anthropic Claude API 地址（可选）
ANTHROPIC_URL=

# WebDAV 白名单端点（可选）
WHITE_WEBDAV_ENDPOINTS=

# SiliconFlow API 密钥（可选）
SILICONFLOW_API_KEY=

# SiliconFlow API 地址（可选）
SILICONFLOW_URL=

# 302.AI API 密钥（可选）
AI302_API_KEY=

# 302.AI API 地址（可选）
AI302_URL=

